// interfaces/index.ts
export interface PillProps {
  title: string; // Defines the expected type for the title prop
}
export interface ButtonProps {
  title: string; // Text displayed on the button
  styleClass: string; // Class for styles (e.g., size and shape)
}
